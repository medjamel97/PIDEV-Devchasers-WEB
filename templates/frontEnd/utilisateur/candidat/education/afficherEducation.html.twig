{% extends '/frontEnd/base.html.twig' %}
{% block content %}
    <section class="bg-home"
             style="background: url('https://via.placeholder.com/2000X1333//88929f/5a6270C/O https://placeholder.com/') center center;">
        <div class="bg-overlay"></div>
        <div class="home-center">
            <div class="home-desc-center">
                <div class="container">
                    <div class="row justify-content-center">
                        <div class="col-lg-12">
                            <div class="title-heading text-center text-white">
                                <h1 class="heading font-weight-bold mb-4">Education</h1>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <div class="col-md-10 p-5">
        <table border="2" class="table">
            <tr>
                {% set labels =["ID","Description", "Niveau Education", "filiere", "Etablissement", "Ville", "Duree", "Candidat"] %}
                {% for i in 0..labels|length-1 %}
                    <td><b>{{ labels[i] }}</b></td>
                {% endfor %}
                <td><b>Operations</b></td>
                {% for education in educations %}
            <tr>
                <td>{{ education.id }}</td>
                <td>{{ education.description }}</td>
                <td>{{ education.niveauEducation }}</td>
                <td>{{ education.filiere }}</td>
                <td>{{ education.etablissement }}</td>
                <td>{{ education.ville }}</td>
                <td>{{ education.duree }}</td>
                <td>{{ education.candidat }}</td>
                <td>
                    <a href="{{ asset('candidat/education='~education.id~'/modifier/') }}">Modifier</a>
                    <a href="{{ asset('candidat/education='~education.id~'/supprimer/') }}">Supprimer</a>
                </td>
            </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}