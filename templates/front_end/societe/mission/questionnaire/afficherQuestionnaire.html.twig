{% extends '/frontEnd/base.html.twig' %}
{% block content %}
    <section class="bg-home"
             style="background: url('https://via.placeholder.com/2000X1333//88929f/5a6270C/O https://placeholder.com/') center center;">
        <div class="bg-overlay"></div>
        <div class="home-center">
            <div class="home-desc-center">
                <div class="container">
                    <div class="row justify-content-center">
                        <div class="col-lg-12">
                            <div class="title-heading text-center text-white">
                                <h1 class="heading font-weight-bold mb-4">Questionnaires</h1>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <div class="col-md-10 p-5">
        <table border="2" class="table">
            <tr>
                {% set labels =["ID", "Candidature Mission"] %}
                {% for i in 0..labels|length-1 %}
                    <td><b>{{ labels[i] }}</b></td>
                {% endfor %}
                <td><b>Operations</b></td>
                {% for questionnaire in questionnaires %}
            <tr>
                <td>{{ questionnaire.id }}</td>
                <td>{{ questionnaire.nom }}</td>
                <td>{{ questionnaire.prenom }}</td>
                <td>{{ questionnaire.dateNaissance|date("d/m/Y") }}</td>
                <td>{{ questionnaire.sexe }}</td>
                <td>{{ questionnaire.tel }}</td>
                <td>{{ questionnaire.idPhoto }}</td>
                <td>
                    <a href="{{ asset('societe/mission/questionnaire='~questionnaire.id~'/modifier/') }}">Modifier</a>
                    <a href="{{ asset('societe/mission/questionnaire='~questionnaire.id~'/Supprimer/') }}">Supprimer</a>
                </td>
            </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}